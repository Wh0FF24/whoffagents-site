import {chromium} from '@playwright/test';const b=await chromium.launch({channel:'chrome'});const p=await b.newPage();p.on('pageerror',e=>console.log(e.message));p.on('console',m=>{if(m.type()==='error')console.log(m.text())});await p.goto('http://127.0.0.1:4173/');await p.waitForTimeout(1500);await p.keyboard.press('Shift');await p.waitForTimeout(2500);console.log(await p.locator('.py-hero').getAttribute('data-scene'));console.log(await p.locator('canvas').count());await b.close();
